const $=id=>document.getElementById(id);let token=localStorage.getItem('awVisitorToken')||'';let state=null;
async function api(url,options={}){options.headers={...(options.headers||{}),'Content-Type':'application/json'};if(token)options.headers['X-Visitor-Token']=token;const response=await fetch(url,options);if(!response.ok)throw new Error(await response.text());return response.status===202?null:response.json()}
async function register(){const visitor=await api('/api/visitors/register',{method:'POST',body:JSON.stringify({alias:$('alias').value,publicAliasConsent:$('publicAlias').checked})});token=visitor.token;localStorage.setItem('awVisitorToken',token);await refresh();await loadVisitors()}
async function vote(decisionId,optionId){document.querySelectorAll('.option').forEach(x=>x.disabled=true);await api(`/api/decisions/${decisionId}/votes`,{method:'POST',body:JSON.stringify({optionId})});await refresh()}
function render(){const visitor=state?.visitor;$('profile').classList.toggle('hidden',!!visitor);$('forget').classList.toggle('hidden',!visitor);$('identity').textContent=visitor?`Welcome back, ${visitor.displayName}. Your choices have shaped ${visitor.storyContributions} office stories.`:'Choose an optional identity to participate.';const decision=state?.activeDecision;$('decision').classList.toggle('hidden',!decision);$('decisionIdle').classList.toggle('hidden',!!decision);if(decision){$('author').textContent=`${decision.authorDisplayName} wants your opinion`;$('question').textContent=decision.question;$('options').innerHTML='';decision.options.forEach(option=>{const button=document.createElement('button');button.className='option';button.disabled=!!state.visitorVoteOptionId;button.innerHTML=`<span>${escapeHtml(option.label)}${state.visitorVoteOptionId===option.optionId?' ✓':''}</span><b>${option.votes}</b>`;button.onclick=()=>vote(decision.decisionId,option.optionId);$('options').appendChild(button)});const seconds=Math.max(0,Math.ceil((decision.closesAtUnixMilliseconds-Date.now())/1000));$('timer').textContent=state.visitorVoteOptionId?`Vote recorded. Result in about ${seconds} seconds.`:`Voting closes in about ${seconds} seconds. The majority changes what happens next.`}const paper=state?.latestNewspaper;$('newspaper').classList.toggle('hidden',!paper);if(paper){$('paperDate').textContent=paper.date;$('headline').textContent=paper.headline;$('summary').textContent=paper.summary;$('stories').innerHTML=paper.stories.map(story=>`<article>${escapeHtml(story)}</article>`).join('');$('quote').textContent=paper.quote;$('decisionResult').textContent=paper.decisionResult;$('visitorThanks').textContent=paper.visitorAcknowledgement;$('teaser').textContent=paper.tomorrowTeaser}const reward=state?.rewards?.[0];$('reward').classList.toggle('hidden',!reward);if(reward){$('rewardName').textContent=reward.displayName;$('rewardCode').textContent=reward.code}}
async function refresh(){try{state=await api('/api/state');render();renderRewardQr()}catch{$('identity').textContent='The virtual office is temporarily unreachable.'}}
function renderRewardQr(){const reward=state?.rewards?.[0];if(!reward)return;const target=`${location.origin}/rewards/${encodeURIComponent(reward.code)}`;$('rewardQr').src=`/api/qr?url=${encodeURIComponent(target)}`}
async function loadVisitors(){try{const values=await api('/api/visitors/public');const recipients=values.filter(x=>x.visitorId!==state?.visitor?.visitorId);$('recipient').innerHTML=recipients.map(x=>`<option value="${x.visitorId}">${escapeHtml(x.displayName)}</option>`).join('');const ready=!!state?.visitor&&recipients.length>0;$('appreciate').disabled=!ready;$('appreciationStatus').textContent=!state?.visitor?'Register your visitor identity first.':recipients.length===0?'No other public visitor aliases are available yet. Open this page in another browser or phone and register a second alias.':`${recipients.length} colleague alias${recipients.length===1?' is':'es are'} available.`}catch{$('appreciationStatus').textContent='Could not load colleague aliases.'}}
async function loadArchive(){try{const papers=await api('/api/newspapers');$('archive').innerHTML=papers.map(p=>`<article><b>${escapeHtml(p.date)}: ${escapeHtml(p.headline)}</b><br>${escapeHtml(p.summary)}</article>`).join('')||'No past editions yet.'}catch{}}
async function appreciate(){const recipient=$('recipient').value;if(!recipient){$('appreciationStatus').textContent='A second opted-in colleague is required.';return}try{await api('/api/appreciation',{method:'POST',body:JSON.stringify({recipientVisitorId:recipient,category:$('category').value,courierAgentId:$('courier').value,publicConsent:$('publicNote').checked})});$('appreciationStatus').textContent='The character accepted and delivered your note.'}catch{$('appreciationStatus').textContent='The note could not be delivered. Refresh and try again.'}}
function escapeHtml(value){const element=document.createElement('span');element.textContent=value||'';return element.innerHTML}
$('register').onclick=register;$('forget').onclick=()=>{localStorage.removeItem('awVisitorToken');token='';state=null;refresh().then(loadVisitors)};$('appreciate').onclick=appreciate;refresh().then(loadVisitors);loadArchive();setInterval(()=>{refresh().then(loadVisitors)},2500);const source=new EventSource('/api/events');source.addEventListener('hub',()=>{refresh().then(loadVisitors);loadArchive()});
